/** Player movement speed in pixels per frame. */
export const PLAYER_SPEED = 2;

/** No movement value. */
export const NO_MOVEMENT = 0;

/** Player initial X position. */
export const PLAYER_INITIAL_X = 100;

/** Player initial Y position. */
export const PLAYER_INITIAL_Y = 100;

/** Player fill color (red). */
export const PLAYER_COLOR = 0xff_00_00;

/** Local origin for circle drawing (top-left corner of the graphic). */
export const CIRCLE_ORIGIN = 0;
